

  <!-- Content here -->
  

        

    <form action="" method="post">
        <input type="text" name="name" placeholder=<?php echo $this->locale->_("name") ?>>
        <input type="text" name="email" placeholder=<?php echo $this->locale->_("email") ?>>
        <input type="text" name="password" placeholder=<?php echo $this->locale->_("password") ?>>
        <input type="text" name="role" placeholder=<?php echo $this->locale->_("role") ?>>
        <input type="submit" name="submit" value=<?php echo $this->locale->_("submit") ?>>
    </form>

    <?php
    $message =$this->request->getQuery('key');
    if (isset($message) && $message!='') {

     

       echo "$message<br><br><br>
       for  products : <strong>/api/products?key={TOKEN}</strong>
       <br>
       for  search product : <strong>/api/products/{page}/{limit}?key={TOKEN}</strong>
       <br>
       for product in limit : <strong>/api/products/search/{name}?key={TOKEN}</strong>
       <br>
       for jump to  nth page : <strong>/api/products/search/{name}/{limit}?key={TOKEN}</strong><br><br>";
    }

    ?>

